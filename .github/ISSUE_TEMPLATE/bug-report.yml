name: Reporte de Defecto
description: Plantilla para reportar defectos encontrados durante la ejecucion de casos de prueba
title: "[BUG-NN] TC-XX — descripcion del defecto"
labels: ["bug", "status:pending"]
assignees: []
body:
  - type: input
    id: bug-id
    attributes:
      label: ID del Defecto
      description: Identificador unico del bug. Formato BUG-NN.
      placeholder: BUG-01
    validations:
      required: true

  - type: input
    id: tc-origen
    attributes:
      label: Caso de Prueba que detecto el defecto
      description: ID del issue del test case. Incluir link al issue (#NN).
      placeholder: "TC-03 (ver #12)"
    validations:
      required: true

  - type: input
    id: hu-afectada
    attributes:
      label: HU afectada
      description: ID de la Historia de Usuario involucrada.
      placeholder: "HU-NN — descripcion corta"
    validations:
      required: true

  - type: dropdown
    id: severidad
    attributes:
      label: Severidad
      description: Impacto del defecto sobre el sistema.
      options:
        - critical
        - major
        - minor
        - trivial
    validations:
      required: true

  - type: dropdown
    id: prioridad
    attributes:
      label: Prioridad de Correccion
      options:
        - alta
        - media
        - baja
    validations:
      required: true

  - type: dropdown
    id: reproducibilidad
    attributes:
      label: Reproducibilidad
      options:
        - siempre
        - intermitente
        - una vez
    validations:
      required: true

  - type: textarea
    id: pasos-reproducir
    attributes:
      label: Pasos para Reproducir
      description: Minimos pasos necesarios para que el defecto ocurra. Numerados.
      placeholder: |
        1. Accion concreta para llegar al estado donde el defecto ocurre.
        2. Siguiente accion.
        3. Trigger exacto del defecto.
    validations:
      required: true

  - type: textarea
    id: resultado-esperado
    attributes:
      label: Resultado Esperado
      description: Comportamiento correcto segun el caso de prueba origen.
      placeholder: Comportamiento concreto esperado segun la especificacion del caso de prueba.
    validations:
      required: true

  - type: textarea
    id: resultado-obtenido
    attributes:
      label: Resultado Obtenido
      description: Lo que realmente ocurrio.
      placeholder: Comportamiento defectuoso observado en la ejecucion.
    validations:
      required: true

  - type: dropdown
    id: ambiente
    attributes:
      label: Ambiente donde se reproduce
      options:
        - dev
        - qa
        - staging
        - prod
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: Logs / Stack Trace
      description: Pegar en bloque de codigo. No resumir el error.
      render: text
      placeholder: |
        ERROR 2026-05-01 10:23:41 - NullPointerException at DocumentValidator.java:142
        ...
    validations:
      required: false

  - type: textarea
    id: workaround
    attributes:
      label: Workaround
      description: Solucion temporal si existe. Dejar vacio si no hay.
      placeholder: No existe workaround conocido.
    validations:
      required: false

  - type: textarea
    id: evidencia
    attributes:
      label: Evidencia
      description: Capturas, video corto (.mp4 < 10MB), payload de request. Arrastrar archivos aqui.
      placeholder: Adjuntar capturas del comportamiento incorrecto.
    validations:
      required: false
